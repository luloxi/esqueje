// Placeholder — MeshProvider removed in favor of direct BrowserWallet (CIP-30).
// Kept for future providers (analytics, theme, etc).
export default function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
